# Vale config. Enforces the OpsCoach house writing rules on the docs.
# Run locally with `vale README.md docs/`; CI runs it on every push (.github/workflows/docs-lint.yml).
StylesPath = styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = OpsCoach
