# Vale config: enforces the OpsCoach house writing rules on all Markdown.
# What each rule checks is documented in docs/doc-style.md.
# Run locally: vale .   (CI runs it via .github/workflows/docs-lint.yml)
StylesPath = styles
MinAlertLevel = suggestion

[*.md]
BasedOnStyles = OpsCoach
